#
# Exchange Aggregations
#

<exchange>.bid_requests       (60)  = sum server.<exchange>.*.bid_request
<exchange>.timeouts           (60)  = sum server.<exchange>.*.timeouts
<exchange>.avg_response_time  (60)  = avg server.<exchange>.*.rt
